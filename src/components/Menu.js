import React from 'react';

const Menu = () => {
    return(
        <section className="menu">
            <span>logo</span>

            <span>file</span>
            <span>folder</span>
            <span>upload</span>
        </section>
    );
}

export default Menu;